name: standard_repositories
description: A new standard for repositories in your Flutter applications.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  collection: ^1.18.0
  meta: ^1.15.0
  rxdart: ^0.28.0

dev_dependencies:
  coverage: ^1.10.0
  mocktail: ^1.0.4
  test: ^1.25.8
  very_good_analysis: ^6.0.0
